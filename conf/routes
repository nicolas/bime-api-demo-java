# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /               controllers.Bime.index()
GET     /connect        controllers.Bime.connect()
GET     /callback       controllers.Bime.callback()
GET     /actions        controllers.Bime.actions()
GET     /dashboards     controllers.Bime.dashboards()
GET     /connections    controllers.Bime.connections()